package constants

/**
  * Created by linsixin on 2017/9/18.
  */
object ConfigNames {
  val idleThreshold = "idleThreshold"
  val maxConnection = "maxConnection"
  val connectionRequestTimeout = "connectionRequestTimeout"
  val connectTimeout = "connectTimeout"
  val readTimeout = "readTimeout"
  val _443ReadTimeout = "_443ReadTimeout"
  val expire = "expire"
  val requestThreadCount = "requestThreadCount"
  val port="port"
}
